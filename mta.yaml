_schema-version: "3.2"
ID: repoproduct
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: repoproduct-destination-content
  type: com.sap.application.content
  requires:
  - name: repoproduct-destination-service
    parameters:
      content-target: true
  - name: repoproduct-repo-host
    parameters:
      service-key:
        name: repoproduct-repo-host-key
  - name: repoproduct-uaa
    parameters:
      service-key:
        name: repoproduct-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: repoproduct_html_repo_host
          ServiceInstanceName: repoproduct-html5-srv
          ServiceKeyName: repoproduct-repo-host-key
          sap.cloud.service: repoproduct
        - Authentication: OAuth2UserTokenExchange
          Name: repoproduct_uaa
          ServiceInstanceName: repoproduct-xsuaa-srv
          ServiceKeyName: repoproduct-uaa-key
          sap.cloud.service: repoproduct
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: repoproduct-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: repoproduct-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - repoproduct.zip
      name: repoproduct
      target-path: resources/
- name: repoproduct
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: repoproduct-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: repoproduct-destination-service
    service-plan: lite
- name: repoproduct-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: repoproduct-xsuaa-srv
    service-plan: application
- name: repoproduct-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: repoproduct-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
